{"config":{"lang":["en"],"separator":"[\\s\\-,:!=\\[\\]()\"`/]+|\\.(?!\\d)|&[lg]t;|(?!\\b)(?=[A-Z][a-z])","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"aias","text":"<p>Schedule AIA prompts as cron jobs \u2014 no config file, no daemon.</p> <p><code>aias</code> turns any AIA prompt into a recurring batch job by reading a <code>schedule:</code> key directly from its YAML frontmatter. It scans your prompts directory, validates each scheduled prompt, and installs the results into your crontab via the <code>whenever</code> gem.</p>"},{"location":"#how-it-works","title":"How It Works","text":"<pre><code>AIA prompt files  \u2500\u2500\u25ba  PromptScanner  \u2500\u2500\u25ba  Validator  \u2500\u2500\u25ba  JobBuilder  \u2500\u2500\u25ba  CrontabManager\n(YAML frontmatter)      grep + parse       syntax +          whenever          crontab block\n                                           binary check       DSL string        replace\n                                                                                    \u2502\n                                                                                    \u25bc\n                                                                            OS cron daemon\n                                                                     aia &lt;prompt_id&gt; &gt;&gt; log 2&gt;&amp;1\n</code></pre>"},{"location":"#core-design-principles","title":"Core Design Principles","text":"<p>Self-describing prompts. A prompt that wants to run on a schedule says so in its own frontmatter. No external config file maps prompts to schedules.</p> <p>Full sync on every <code>update</code>. The entire <code>whenever</code>-managed crontab block is replaced on each run. Deleted or de-scheduled prompts are automatically removed \u2014 there are no orphaned entries to clean up manually.</p> <p>OS cron reliability. No long-running daemon. The system cron daemon handles job execution, reboots, and missed runs. Each <code>aia</code> invocation is a fresh, clean process.</p> <p>Login shell wrapping. Every cron entry runs inside a login shell (<code>bash -l -c</code> or the user's <code>$SHELL</code>), so rbenv, PATH, and all AIA environment variables are available automatically.</p>"},{"location":"#at-a-glance","title":"At a Glance","text":"<pre><code># Add schedule: to a prompt's frontmatter, then:\naias update     # install all scheduled prompts into crontab\naias check      # diff: prompts with schedule: vs what is installed\naias list       # show all installed jobs\naias dry-run    # preview what update would write\n</code></pre> <p>See the Quick Start to get running in under five minutes.</p>"},{"location":"cli/","title":"CLI Reference","text":"<p><code>aias</code> provides eight commands. All are available via <code>aias help</code> at the terminal.</p> <pre><code>aias help\n</code></pre>"},{"location":"cli/#command-summary","title":"Command Summary","text":"Command Purpose <code>aias update</code> Scan prompts, validate, install all scheduled jobs. Primary command. <code>aias add PATH</code> Add or replace a single prompt's cron job without touching others. <code>aias check</code> Diff view: what is scheduled vs what is installed. <code>aias list</code> Print all currently installed jobs. <code>aias dry-run</code> Preview <code>update</code> output without writing the crontab. <code>aias show PROMPT_ID</code> Inspect a single installed job. <code>aias next [N]</code> Show schedule and last-run information for installed jobs. <code>aias clear</code> Remove all aias-managed crontab entries."},{"location":"cli/#global-option","title":"Global Option","text":"<p><code>--prompts-dir PATH</code> (alias <code>-p</code>) overrides the <code>AIA_PROMPTS__DIR</code> / <code>AIA_PROMPTS_DIR</code> environment variables for any command that reads prompt files. It has no effect on commands that only read from the crontab (<code>list</code>, <code>show</code>, <code>next</code>, <code>clear</code>).</p> <pre><code>aias --prompts-dir ~/work/prompts update\naias --prompts-dir ~/work/prompts add ~/work/prompts/standup.md\naias -p ~/work/prompts dry-run\naias -p ~/work/prompts check\n</code></pre> <p>The lookup order for the prompts directory is:</p> <ol> <li><code>--prompts-dir</code> CLI option (highest priority)</li> <li><code>AIA_PROMPTS__DIR</code> environment variable (AIA &gt;= 0.8.0)</li> <li><code>AIA_PROMPTS_DIR</code> environment variable (AIA &lt; 0.8.0)</li> <li>Error \u2014 neither option nor env var is set</li> </ol>"},{"location":"cli/#exit-codes","title":"Exit Codes","text":"Code Meaning <code>0</code> Success <code>1</code> Error (bad prompts directory, crontab write failure, prompt not found) <p>Validation failures during <code>update</code> do not cause a non-zero exit \u2014 invalid prompts are warned and skipped while valid ones are installed.</p>"},{"location":"cli/add/","title":"aias add","text":"<p>Add or replace a single prompt's cron job without touching any other installed jobs.</p> <pre><code>aias add PATH\n</code></pre> <p><code>PATH</code> is the path to the prompt file \u2014 absolute or relative. The file must live inside the configured prompts directory.</p>"},{"location":"cli/add/#what-it-does","title":"What It Does","text":"<ol> <li>Expands <code>PATH</code> to an absolute path</li> <li>Verifies the file exists and is inside the prompts directory</li> <li>Parses the file's YAML frontmatter via <code>PM::Metadata</code></li> <li>Errors immediately if the file has no <code>schedule:</code> key</li> <li>Runs the same validation as <code>update</code> (schedule syntax, parameter defaults, <code>aia</code> binary)</li> <li>Derives the prompt ID from the file's path relative to the prompts directory</li> <li>Upserts the single cron entry into the aias-managed crontab block \u2014 if the prompt was already installed, its old entry is replaced; all other entries are left untouched</li> </ol>"},{"location":"cli/add/#when-to-use-add-vs-update","title":"When to Use <code>add</code> vs <code>update</code>","text":"Situation Command You edited or created one prompt and want it scheduled now <code>aias add PATH</code> You edited multiple prompts, or want to sync everything <code>aias update</code> You want to see what would change first <code>aias dry-run</code> or <code>aias check</code> <p><code>add</code> is faster than <code>update</code> for large prompts directories because it does not scan the entire tree \u2014 it only reads the one file you specify.</p>"},{"location":"cli/add/#example-output","title":"Example Output","text":"<p>Success:</p> <pre><code>aias: added standup (every weekday at 9:00am (0 9 * * 1-5))\n</code></pre> <p>Prompt has no schedule:</p> <pre><code>aias [error] 'drafts/idea' has no schedule: in its frontmatter\n</code></pre> <p>Invalid schedule:</p> <pre><code>aias [error] bad_prompt: Schedule 'every banana': not a valid cron expression or natural language schedule\n</code></pre> <p>File outside prompts directory:</p> <pre><code>aias [error] '/tmp/idea.md' is not inside the prompts directory '/Users/you/.prompts'\n</code></pre>"},{"location":"cli/add/#upsert-semantics","title":"Upsert Semantics","text":"<p>Re-running <code>add</code> on the same prompt replaces its cron entry cleanly \u2014 no duplicates are created. This makes <code>add</code> safe to run any number of times:</p> <pre><code># Install:\naias add ~/.prompts/standup.md\n# aias: added standup (every weekday at 9:00am (0 9 * * 1-5))\n\n# Update the schedule in the file, then re-install:\naias add ~/.prompts/standup.md\n# aias: added standup (every weekday at 10:00am (0 10 * * 1-5))\n</code></pre>"},{"location":"cli/add/#prompt-id-derivation","title":"Prompt ID Derivation","text":"<p>The prompt ID is derived the same way as <code>update</code>: the file's absolute path with the prompts directory prefix and <code>.md</code> extension removed.</p> <pre><code>prompts_dir:  /Users/you/.prompts\nfile:         /Users/you/.prompts/reports/weekly.md\nprompt_id:    reports/weekly\n</code></pre>"},{"location":"cli/add/#options","title":"Options","text":"Option Alias Description <code>--prompts-dir PATH</code> <code>-p</code> Use PATH as the prompts directory (also determines the prompt ID prefix) <p>When <code>--prompts-dir</code> is not supplied, the prompts directory is read from <code>AIA_PROMPTS__DIR</code> or <code>AIA_PROMPTS_DIR</code>. The same value is embedded in the generated <code>aia</code> command so the cron job runs against the correct directory.</p>"},{"location":"cli/add/#exit-codes","title":"Exit Codes","text":"Code Condition <code>0</code> Job installed successfully <code>1</code> File not found, outside prompts dir, no <code>schedule:</code>, validation failure, or crontab write error"},{"location":"cli/add/#see-also","title":"See Also","text":"<ul> <li><code>aias update</code> \u2014 sync all scheduled prompts at once</li> <li><code>aias check</code> \u2014 diff view before making changes</li> <li><code>aias list</code> \u2014 confirm the job was installed</li> <li>Scheduling Prompts \u2014 schedule format reference</li> </ul>"},{"location":"cli/check/","title":"aias check","text":"<p>Diff view: compare prompts that declare <code>schedule:</code> against what is currently installed in the crontab.</p> <pre><code>aias check\n</code></pre>"},{"location":"cli/check/#what-it-shows","title":"What It Shows","text":"<p><code>check</code> categorises every prompt and every installed job into one of four states:</p> Label Meaning <code>OK</code> Crontab is in sync \u2014 nothing to report <code>NEW</code> Prompt has <code>schedule:</code> but is not yet installed \u2014 run <code>update</code> <code>ORPHANED</code> Installed in crontab but no corresponding prompt found \u2014 run <code>update</code> to remove <code>INVALID</code> Prompt has <code>schedule:</code> but failed validation \u2014 shown with error details"},{"location":"cli/check/#example-output","title":"Example Output","text":"<p>In sync:</p> <pre><code>=== aias check ===\n\nOK \u2014 crontab is in sync with scheduled prompts\n</code></pre> <p>Changes needed:</p> <pre><code>=== aias check ===\n\nINVALID (would be skipped by update):\n  bad_prompt: Schedule 'every banana': undefined method 'banana' for ...\n\nNEW (not yet installed \u2014 run `aias update`):\n  + reports/weekly\n\nORPHANED (installed but no longer scheduled):\n  - old_daily_job\n</code></pre>"},{"location":"cli/check/#typical-workflow","title":"Typical Workflow","text":"<p>Run <code>check</code> after editing prompt files to see what <code>update</code> would change before actually running it:</p> <pre><code># Edit a prompt's schedule:\nvim $AIA_PROMPTS_DIR/daily_digest.md\n\n# Preview the impact:\naias check\n\n# Apply:\naias update\n</code></pre>"},{"location":"cli/check/#options","title":"Options","text":"Option Alias Description <code>--prompts-dir PATH</code> <code>-p</code> Use PATH instead of <code>AIA_PROMPTS__DIR</code> / <code>AIA_PROMPTS_DIR</code>"},{"location":"cli/check/#see-also","title":"See Also","text":"<ul> <li><code>aias update</code> \u2014 apply the changes shown by <code>check</code></li> <li><code>aias dry-run</code> \u2014 preview the generated cron lines</li> <li><code>aias list</code> \u2014 see all currently installed jobs</li> </ul>"},{"location":"cli/clear/","title":"aias clear","text":"<p>Remove all aias-managed crontab entries.</p> <pre><code>aias clear\n</code></pre>"},{"location":"cli/clear/#what-it-does","title":"What It Does","text":"<p>Calls <code>whenever --clear</code> for the <code>aias</code> identifier, removing the entire <code>aias</code>-managed block from the crontab. All other crontab entries (your own manually added jobs or other <code>whenever</code>-managed blocks) are left untouched.</p>"},{"location":"cli/clear/#output","title":"Output","text":"<pre><code>aias: all managed crontab entries removed\n</code></pre>"},{"location":"cli/clear/#use-cases","title":"Use Cases","text":"<p>Temporarily disable all scheduled prompts without editing any prompt files:</p> <pre><code>aias clear\n# Re-enable later:\naias update\n</code></pre> <p>Reset before a clean reinstall:</p> <pre><code>aias clear\naias update\n</code></pre>"},{"location":"cli/clear/#notes","title":"Notes","text":"<p><code>clear</code> does not validate prompts or read <code>$AIA_PROMPTS_DIR</code>. It simply removes whatever <code>aias</code> block is currently in the crontab.</p> <p>After <code>clear</code>, <code>aias list</code> returns \"no installed jobs\" and <code>aias check</code> shows all scheduled prompts as <code>NEW</code>.</p>"},{"location":"cli/clear/#see-also","title":"See Also","text":"<ul> <li><code>aias update</code> \u2014 reinstall after clearing</li> <li><code>aias list</code> \u2014 verify the crontab is empty after clearing</li> </ul>"},{"location":"cli/dry-run/","title":"aias dry-run","text":"<p>Show what <code>aias update</code> would write to the crontab without making any changes.</p> <pre><code>aias dry-run\n</code></pre>"},{"location":"cli/dry-run/#output","title":"Output","text":"<p>The raw cron lines that would be installed, exactly as they would appear in the crontab:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia daily_digest &gt;&gt; /Users/you/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n0 9 * * 1 /bin/zsh -l -c 'aia reports/weekly &gt;&gt; /Users/you/.aia/schedule/logs/reports/weekly.log 2&gt;&amp;1'\n</code></pre> <p>Invalid prompts are printed to stderr (same as <code>update</code>):</p> <pre><code>aias [skip] bad_prompt: Schedule 'every banana': ...\n</code></pre> <p>When no valid prompts are found:</p> <pre><code>aias: no valid scheduled prompts found\n</code></pre>"},{"location":"cli/dry-run/#use-cases","title":"Use Cases","text":"<p>Verify the generated cron expression before committing it to your crontab:</p> <pre><code>aias dry-run\n# Inspect the output, then:\naias update\n</code></pre> <p>Check login shell wrapping \u2014 confirm the correct shell binary and <code>-l</code> flag appear in the output.</p> <p>Audit from CI \u2014 <code>dry-run</code> is safe to run in automated pipelines because it never modifies the crontab.</p>"},{"location":"cli/dry-run/#notes","title":"Notes","text":"<p><code>dry-run</code> runs the full scan and validation pipeline. The only thing it skips is the final crontab write. This means it accurately reflects what <code>update</code> would do, including which prompts would be skipped.</p>"},{"location":"cli/dry-run/#options","title":"Options","text":"Option Alias Description <code>--prompts-dir PATH</code> <code>-p</code> Use PATH instead of <code>AIA_PROMPTS__DIR</code> / <code>AIA_PROMPTS_DIR</code>"},{"location":"cli/dry-run/#see-also","title":"See Also","text":"<ul> <li><code>aias update</code> \u2014 apply the output shown by <code>dry-run</code></li> <li><code>aias check</code> \u2014 higher-level diff view</li> </ul>"},{"location":"cli/list/","title":"aias list","text":"<p>Print all currently installed aias cron jobs.</p> <pre><code>aias list\n</code></pre>"},{"location":"cli/list/#output","title":"Output","text":"<pre><code>PROMPT ID                       SCHEDULE                                  LOG\n----------------------------------------------------------------------------------------------------\ndaily_digest                    every day at 8am (0 8 * * *)              /Users/you/.aia/schedule/logs/daily_digest.log\nreports/weekly                  every Monday at 9am (0 9 * * 1)           /Users/you/.aia/schedule/logs/reports/weekly.log\n</code></pre> <p>When no jobs are installed:</p> <pre><code>aias: no installed jobs\n</code></pre>"},{"location":"cli/list/#columns","title":"Columns","text":"Column Description <code>PROMPT ID</code> Full subpath relative to <code>$AIA_PROMPTS_DIR</code>, without the <code>.md</code> extension <code>SCHEDULE</code> Human-readable description with raw cron expression in parentheses, e.g. <code>every day at 8am (0 8 * * *)</code> <code>LOG</code> Absolute path to the log file for this job"},{"location":"cli/list/#notes","title":"Notes","text":"<p><code>list</code> reads directly from the crontab \u2014 it shows what is actually installed, not what the current prompt files declare. Use <code>aias check</code> to see whether the installed jobs match the current prompt files.</p>"},{"location":"cli/list/#see-also","title":"See Also","text":"<ul> <li><code>aias check</code> \u2014 diff between prompts and installed jobs</li> <li><code>aias show PROMPT_ID</code> \u2014 details for a single job</li> <li><code>aias next</code> \u2014 schedule and last-run information</li> </ul>"},{"location":"cli/next/","title":"aias next","text":"<p>Show schedule and last-run information for all installed jobs.</p> <pre><code>aias next [N]\n</code></pre> <p>The command is also accessible as <code>aias upcoming</code> (the Ruby method name, since <code>next</code> is a reserved keyword).</p>"},{"location":"cli/next/#arguments","title":"Arguments","text":"Argument Default Description <code>N</code> <code>5</code> Ignored in v1 (reserved for future next-run time calculation)"},{"location":"cli/next/#output","title":"Output","text":"<pre><code>daily_digest\n  schedule : every day at 8am (0 8 * * *)\n  last run : 2026-03-21 08:00:01 +0000\n  log      : /Users/you/.aia/schedule/logs/daily_digest.log\n\nreports/weekly\n  schedule : every Monday at 9am (0 9 * * 1)\n  last run : never run\n  log      : /Users/you/.aia/schedule/logs/reports/weekly.log\n\n(Pass N as argument to show N entries. Next-run times require `fugit` gem \u2014 v1 shows log timestamps instead.)\n</code></pre> <p>When no jobs are installed:</p> <pre><code>aias: no installed jobs\n</code></pre>"},{"location":"cli/next/#notes","title":"Notes","text":"<p>Last run time is derived from the log file's modification timestamp. If the log file does not exist, \"never run\" is shown.</p> <p>Next run time calculation requires the <code>fugit</code> gem, which is not a dependency of v1. The argument <code>N</code> is accepted but ignored in this release.</p>"},{"location":"cli/next/#see-also","title":"See Also","text":"<ul> <li><code>aias list</code> \u2014 tabular view of installed jobs</li> <li><code>aias show PROMPT_ID</code> \u2014 details for a single job</li> </ul>"},{"location":"cli/show/","title":"aias show","text":"<p>Show the installed crontab entry for a single prompt.</p> <pre><code>aias show PROMPT_ID\n</code></pre>"},{"location":"cli/show/#arguments","title":"Arguments","text":"Argument Description <code>PROMPT_ID</code> The prompt ID as it appears in <code>aias list</code> (full subpath, no <code>.md</code> extension)"},{"location":"cli/show/#output","title":"Output","text":"<pre><code>aias show daily_digest\n</code></pre> <pre><code>prompt_id : daily_digest\nschedule  : every day at 8am (0 8 * * *)\nlog       : /Users/you/.aia/schedule/logs/daily_digest.log\n</code></pre> <p>For a nested prompt:</p> <pre><code>aias show reports/weekly\n</code></pre> <pre><code>prompt_id : reports/weekly\nschedule  : every Monday at 9am (0 9 * * 1)\nlog       : /Users/you/.aia/schedule/logs/reports/weekly.log\n</code></pre>"},{"location":"cli/show/#error","title":"Error","text":"<p>When the prompt ID is not found in the crontab, <code>show</code> prints a message and exits 1:</p> <pre><code>aias: 'nonexistent' is not currently installed\n</code></pre>"},{"location":"cli/show/#notes","title":"Notes","text":"<p><code>show</code> reads from the installed crontab, not from the prompt files. A prompt must have been installed via <code>aias update</code> before it appears.</p>"},{"location":"cli/show/#see-also","title":"See Also","text":"<ul> <li><code>aias list</code> \u2014 all installed jobs</li> <li><code>aias check</code> \u2014 compare installed vs scheduled</li> </ul>"},{"location":"cli/update/","title":"aias update","text":"<p>Full sync: scan all prompts, validate, and install every scheduled prompt as a cron job.</p> <pre><code>aias update\n</code></pre>"},{"location":"cli/update/#what-it-does","title":"What It Does","text":"<ol> <li>Scans <code>$AIA_PROMPTS_DIR</code> recursively for files containing <code>schedule:</code> in their frontmatter</li> <li>Parses the YAML frontmatter of each candidate file via <code>PM::Metadata</code></li> <li>Validates each scheduled prompt (schedule syntax, parameter defaults, <code>aia</code> binary)</li> <li>Builds a <code>whenever</code> DSL string for each valid prompt</li> <li>Combines all DSL strings and calls <code>whenever</code> to replace the entire <code>aias</code>-managed crontab block</li> </ol>"},{"location":"cli/update/#output","title":"Output","text":"<pre><code>aias: installed 3 job(s)\n</code></pre> <p>When some prompts are invalid:</p> <pre><code>aias: installed 2 job(s), skipped 1 invalid\n</code></pre> <p>Invalid prompts are printed to stderr:</p> <pre><code>aias [skip] bad_prompt: Schedule 'every banana': ...\n</code></pre> <p>When no valid prompts are found:</p> <pre><code>aias: no valid scheduled prompts found \u2014 crontab not changed\n</code></pre>"},{"location":"cli/update/#full-sync-semantics","title":"Full Sync Semantics","text":"<p>Every <code>update</code> replaces the entire <code>aias</code>-managed crontab block. This means:</p> <ul> <li>Prompts that had their <code>schedule:</code> line removed are automatically uninstalled</li> <li>Schedule changes take effect on the next <code>update</code></li> <li>No manual cleanup of orphaned entries is ever required</li> </ul> <p>Non-aias crontab entries are never touched.</p>"},{"location":"cli/update/#error-handling","title":"Error Handling","text":"Condition Behaviour <code>AIA_PROMPTS_DIR</code> not set or missing Exits 1 with an error message Crontab write fails Exits 1 with an error message Invalid schedule syntax Skips the prompt, warns to stderr Missing parameter defaults Skips the prompt, warns to stderr <code>aia</code> binary not found Skips all prompts, warns to stderr"},{"location":"cli/update/#options","title":"Options","text":"Option Alias Description <code>--prompts-dir PATH</code> <code>-p</code> Use PATH instead of <code>AIA_PROMPTS__DIR</code> / <code>AIA_PROMPTS_DIR</code> <pre><code>aias --prompts-dir ~/work/prompts update\n</code></pre>"},{"location":"cli/update/#see-also","title":"See Also","text":"<ul> <li><code>aias dry-run</code> \u2014 preview without writing</li> <li><code>aias check</code> \u2014 see what would change</li> <li>Scheduling Prompts \u2014 schedule format reference</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"Requirement Notes Ruby &gt;= 3.2.0 <code>aia</code> gem Must be installed and reachable via <code>which aia</code> in your login shell <code>AIA_PROMPTS_DIR</code> Environment variable pointing to your AIA prompts directory"},{"location":"getting-started/installation/#install-the-gem","title":"Install the Gem","text":"<pre><code>gem install aias\n</code></pre> <p>Or add it to your project's <code>Gemfile</code>:</p> <pre><code>gem \"aias\"\n</code></pre> <p>Then run:</p> <pre><code>bundle install\n</code></pre>"},{"location":"getting-started/installation/#verify-prerequisites","title":"Verify Prerequisites","text":"<p><code>aias update</code> will validate that <code>aia</code> is reachable and that <code>AIA_PROMPTS_DIR</code> is set. You can check manually first:</p> <pre><code># Confirm aia is in your login shell PATH\nbash -l -c \"which aia\"\n\n# Confirm AIA_PROMPTS_DIR is set and points to a real directory\necho $AIA_PROMPTS_DIR\nls \"$AIA_PROMPTS_DIR\"\n</code></pre> <p>If <code>aia</code> is not found, install it:</p> <pre><code>gem install aia\n</code></pre>"},{"location":"getting-started/installation/#set-aia_prompts_dir","title":"Set AIA_PROMPTS_DIR","text":"<p>Add to your shell profile (<code>.bashrc</code>, <code>.zshrc</code>, etc.):</p> <pre><code>export AIA_PROMPTS_DIR=\"$HOME/.prompts\"\n</code></pre> <p>Reload your shell or source the file:</p> <pre><code>source ~/.zshrc   # or ~/.bashrc\n</code></pre>"},{"location":"getting-started/installation/#verify-the-installation","title":"Verify the Installation","text":"<pre><code>aias --version\naias help\n</code></pre> <p><code>aias help</code> lists all available commands with descriptions.</p>"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>Get a prompt running on a schedule in under five minutes.</p>"},{"location":"getting-started/quick-start/#step-1-add-a-schedule-key-to-a-prompt","title":"Step 1 \u2014 Add a <code>schedule:</code> Key to a Prompt","text":"<p>Open any AIA prompt file (<code>.md</code> in your <code>$AIA_PROMPTS_DIR</code>) and add a <code>schedule:</code> key to its YAML frontmatter:</p> <pre><code>---\ndescription: Morning digest\nschedule: \"0 8 * * *\"\n---\nSummarize what happened overnight in the Ruby and AI ecosystems.\nKeep the summary under 300 words.\n</code></pre> <p>The <code>schedule:</code> value accepts either a raw cron expression or a whenever DSL string. See Schedule Format for the full syntax.</p>"},{"location":"getting-started/quick-start/#step-2-preview-the-crontab-entry","title":"Step 2 \u2014 Preview the Crontab Entry","text":"<p>Before writing anything, see what <code>aias</code> would install:</p> <pre><code>aias dry-run\n</code></pre> <p>Example output:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia daily_digest &gt;&gt; /Users/you/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n</code></pre>"},{"location":"getting-started/quick-start/#step-3-install","title":"Step 3 \u2014 Install","text":"<pre><code>aias update\n</code></pre> <p><code>aias</code> scans every prompt file, validates the scheduled ones, and replaces the entire <code>aias</code>-managed block in your crontab. Output shows how many jobs were installed and how many (if any) were skipped:</p> <pre><code>aias: installed 1 job(s)\n</code></pre>"},{"location":"getting-started/quick-start/#step-4-confirm","title":"Step 4 \u2014 Confirm","text":"<pre><code>aias list\n</code></pre> <pre><code>PROMPT ID                       SCHEDULE         LOG\n------------------------------  ---------------  -----------------------------------------------\ndaily_digest                    0 8 * * *        /Users/you/.aia/schedule/logs/daily_digest.log\n</code></pre> <pre><code>aias check\n</code></pre> <p>When everything is in sync:</p> <pre><code>=== aias check ===\n\nOK \u2014 crontab is in sync with scheduled prompts\n</code></pre>"},{"location":"getting-started/quick-start/#step-5-add-more-prompts-optional","title":"Step 5 \u2014 Add More Prompts (Optional)","text":"<p>Every <code>aias update</code> is a full sync. Add <code>schedule:</code> to more prompts, then run <code>update</code> again \u2014 new prompts are added, removed prompts are cleaned up automatically.</p> <pre><code># Add more prompts, then:\naias update\n</code></pre>"},{"location":"getting-started/quick-start/#what-happens-at-runtime","title":"What Happens at Runtime","text":"<p>When cron fires the job, it runs:</p> <pre><code>/bin/zsh -l -c 'aia daily_digest &gt;&gt; ~/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n</code></pre> <p>The login shell (<code>-l</code>) ensures rbenv, PATH, and all your environment variables are loaded. stdout and stderr are both appended to the log file.</p>"},{"location":"getting-started/quick-start/#disabling-a-prompt","title":"Disabling a Prompt","text":"<p>Remove or comment out the <code>schedule:</code> line, then run <code>aias update</code>. The orphaned crontab entry is removed automatically.</p>"},{"location":"guides/cron-environment/","title":"Cron Environment","text":"<p>Cron runs in a minimal shell with almost no environment: no <code>PATH</code> beyond <code>/usr/bin:/bin</code>, no rbenv or asdf activation, no user-defined variables. Without intervention, <code>aia</code> would not be found and jobs would silently fail.</p>"},{"location":"guides/cron-environment/#how-aias-solves-this","title":"How aias Solves This","text":"<p>Every cron entry generated by <code>aias</code> wraps the <code>aia</code> invocation in a login shell:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia daily_digest &gt;&gt; ~/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n</code></pre> <p>The <code>-l</code> flag sources your shell's login profile (<code>.zshrc</code>, <code>.bash_profile</code>, etc.), which activates rbenv, loads <code>PATH</code> additions, and sets any environment variables your <code>aia</code> installation depends on.</p>"},{"location":"guides/cron-environment/#shell-detection","title":"Shell Detection","text":"<p>The shell binary is read from <code>ENV[\"SHELL\"]</code> at the time <code>aias update</code> is run. If <code>SHELL</code> is not set, <code>/bin/bash</code> is used as a fallback.</p> <pre><code>echo $SHELL\n# /bin/zsh\n</code></pre> <p>The resulting job template:</p> <pre><code>set :job_template, \"/bin/zsh -l -c ':job'\"\n</code></pre>"},{"location":"guides/cron-environment/#supported-shells","title":"Supported Shells","text":"<p>Any POSIX-compatible shell that supports the <code>-l</code> (login) and <code>-c</code> (command) flags:</p> Shell Works <code>bash</code> Yes <code>zsh</code> Yes <code>fish</code> Yes (fish supports <code>-l -c</code>) <code>sh</code> Yes"},{"location":"guides/cron-environment/#troubleshooting-cron-failures","title":"Troubleshooting Cron Failures","text":"<p>If jobs are failing silently, check the log file:</p> <pre><code>cat ~/.aia/schedule/logs/daily_digest.log\n</code></pre> <p>Common causes:</p> <p><code>aia</code> not found:</p> <pre><code>zsh: command not found: aia\n</code></pre> <p>Fix: ensure <code>aia</code> is installed and <code>gem install aia</code> runs inside your login shell. Verify with:</p> <pre><code>bash -l -c \"which aia\"\n</code></pre> <p><code>AIA_PROMPTS_DIR</code> not set in cron:</p> <p>Cron does not inherit your interactive shell's exports. Set <code>AIA_PROMPTS_DIR</code> in your shell's login profile (<code>.bash_profile</code>, <code>.zprofile</code>), not just in <code>.bashrc</code> or <code>.zshrc</code> (which are only sourced for interactive shells).</p> <pre><code># ~/.zprofile or ~/.bash_profile\nexport AIA_PROMPTS_DIR=\"$HOME/.prompts\"\nexport AIA_API_KEY=\"sk-...\"\n</code></pre> <p>rbenv / asdf not activated:</p> <p>Add rbenv or asdf initialization to your login profile:</p> <pre><code># ~/.zprofile\neval \"$(rbenv init -)\"\n</code></pre>"},{"location":"guides/cron-environment/#environment-variables-in-cron-jobs","title":"Environment Variables in Cron Jobs","text":"<p>All environment variables that <code>aia</code> needs must be set in your login profile. Cron does not read <code>.env</code> files or any project-level configuration.</p> <p>Required variables (typical):</p> Variable Description <code>AIA_PROMPTS_DIR</code> Path to your prompts directory <code>ANTHROPIC_API_KEY</code> or <code>OPENAI_API_KEY</code> API key for the AI backend <p>Any variable set in your login profile is available in <code>aias</code>-scheduled cron jobs.</p>"},{"location":"guides/scheduling-prompts/","title":"Scheduling Prompts","text":""},{"location":"guides/scheduling-prompts/#the-schedule-key","title":"The <code>schedule:</code> Key","text":"<p>Add a single <code>schedule:</code> key to any AIA prompt's YAML frontmatter to make it a scheduled job:</p> <pre><code>---\ndescription: Morning briefing\nschedule: \"0 8 * * *\"\n---\nSummarize what happened overnight.\n</code></pre> <p>The value is a plain string \u2014 either a raw cron expression or a whenever DSL fragment. That is all <code>aias</code> needs.</p>"},{"location":"guides/scheduling-prompts/#enabling-and-disabling","title":"Enabling and Disabling","text":"Action How Enable scheduling Add <code>schedule:</code> to the frontmatter Disable scheduling Remove or comment out the <code>schedule:</code> line Change the schedule Edit the <code>schedule:</code> value, then run <code>aias update</code> <p>After any change, run <code>aias update</code> to sync the crontab.</p>"},{"location":"guides/scheduling-prompts/#schedule-formats","title":"Schedule Formats","text":""},{"location":"guides/scheduling-prompts/#raw-cron-expressions","title":"Raw Cron Expressions","text":"<p>Standard five-field cron syntax and shorthand keywords:</p> <pre><code>schedule: \"0 8 * * *\"      # 8:00 AM every day\nschedule: \"30 6 * * 1-5\"   # 6:30 AM weekdays\nschedule: \"0 */4 * * *\"    # every 4 hours\nschedule: \"@daily\"          # shorthand for 0 0 * * *\nschedule: \"@hourly\"         # shorthand for 0 * * * *\nschedule: \"@weekly\"         # shorthand for 0 0 * * 0\nschedule: \"@monthly\"        # shorthand for 0 0 1 * *\n</code></pre>"},{"location":"guides/scheduling-prompts/#whenever-dsl","title":"Whenever DSL","text":"<p>Human-readable scheduling via the <code>whenever</code> gem's DSL:</p> <pre><code>schedule: \"1.day\"                    # once per day (at midnight)\nschedule: \"1.day, at: '8:00am'\"     # daily at 8 AM\nschedule: \"6.hours\"                  # every 6 hours\nschedule: \"1.week\"                   # weekly\nschedule: \":monday, at: '9:00am'\"   # every Monday at 9 AM\nschedule: \":friday, at: '5:00pm'\"   # every Friday at 5 PM\n</code></pre> <p>Tip</p> <p>When in doubt about whether your schedule string will parse correctly, use <code>aias check</code> or <code>aias dry-run</code> to validate it before running <code>aias update</code>.</p>"},{"location":"guides/scheduling-prompts/#parameters-in-scheduled-prompts","title":"Parameters in Scheduled Prompts","text":"<p>If your prompt uses <code>parameters:</code>, every parameter must have a default value. Cron runs unattended \u2014 there is no interactive session to prompt the user for input.</p> <p>This will fail validation:</p> <pre><code>---\nschedule: \"0 8 * * *\"\nparameters:\n  topic:              # \u2190 no default \u2014 invalid for scheduled prompts\n---\nWrite about &lt;%= topic %&gt;.\n</code></pre> <p>This is valid:</p> <pre><code>---\nschedule: \"0 8 * * *\"\nparameters:\n  topic: \"Ruby news\"  # \u2190 default provided \u2014 always safe to run unattended\n---\nWrite about &lt;%= topic %&gt;.\n</code></pre> <p><code>aias</code> rejects prompts with missing parameter defaults and warns loudly. The prompt is excluded from the crontab install.</p>"},{"location":"guides/scheduling-prompts/#example-nested-prompt","title":"Example: Nested Prompt","text":"<p>Prompts in subdirectories work exactly the same. The full subpath (minus <code>.md</code>) becomes the prompt ID:</p> <pre><code>$AIA_PROMPTS_DIR/\n  reports/\n    weekly_digest.md     \u2192 prompt ID: reports/weekly_digest\n  daily_digest.md        \u2192 prompt ID: daily_digest\n</code></pre> <pre><code># reports/weekly_digest.md\n---\ndescription: Weekly summary of team activity\nschedule: \"0 9 * * 1\"   # every Monday at 9 AM\n---\nSummarize this week's key developments.\n</code></pre> <p>The log file path mirrors the directory structure:</p> <pre><code>~/.aia/schedule/logs/reports/weekly_digest.log\n</code></pre>"},{"location":"guides/scheduling-prompts/#what-gets-passed-to-aia","title":"What Gets Passed to AIA","text":"<p><code>aias</code> passes only the prompt ID to <code>aia</code>:</p> <pre><code>aia daily_digest\naia reports/weekly_digest\n</code></pre> <p>AIA reads the prompt file itself, including any flags, model settings, or roles defined in the frontmatter. <code>aias</code> does not need to know about those.</p>"},{"location":"guides/validation/","title":"Validation","text":"<p>Before writing anything to the crontab, <code>aias</code> validates every scheduled prompt. This prevents broken cron entries from silently failing at runtime.</p>"},{"location":"guides/validation/#validation-rules","title":"Validation Rules","text":""},{"location":"guides/validation/#1-schedule-syntax","title":"1. Schedule Syntax","text":"<p>The <code>schedule:</code> value must be parseable as either a cron expression or a whenever DSL string.</p> <p><code>aias</code> validates this by evaluating a minimal whenever DSL fragment:</p> <pre><code>every '0 8 * * *' do\n  command 'true'\nend\n</code></pre> <p>Valid examples:</p> <pre><code>schedule: \"0 8 * * *\"\nschedule: \"@daily\"\nschedule: \"every 1.day at 8:00am\"\nschedule: \"every :monday at 9:00am\"\nschedule: \"every 6.hours\"\n</code></pre> <p>Invalid examples (fail with an error):</p> <pre><code>schedule: \"every banana\"     # not valid whenever DSL\nschedule: \"0 8 *\"            # too few cron fields\nschedule: \"\"                 # empty\n</code></pre>"},{"location":"guides/validation/#2-parameter-completeness","title":"2. Parameter Completeness","text":"<p>All <code>parameters:</code> keys must have non-nil, non-empty default values. Cron jobs run unattended \u2014 there is no interactive session to supply missing values.</p> <pre><code># VALID \u2014 all parameters have defaults\nparameters:\n  topic: \"Ruby news\"\n  format: \"bullet points\"\n\n# INVALID \u2014 missing default\nparameters:\n  topic:           # nil value \u2014 rejected\n  format: \"\"       # empty string \u2014 rejected\n</code></pre>"},{"location":"guides/validation/#3-aia-binary","title":"3. AIA Binary","text":"<p>The <code>aia</code> binary must be locatable. Two checks are performed in order; if either succeeds, the prompt passes:</p> <ol> <li> <p>Login-shell <code>which</code> \u2014 spawns a login shell and runs <code>which aia</code>. This is the normal path when the shell profile initialises the version manager (rbenv, rvm, asdf, etc.).</p> </li> <li> <p>Fallback directory scan \u2014 if the login-shell check fails (common when <code>rbenv init</code> is not in the bash login profile), <code>aias</code> scans these directories for an executable named <code>aia</code>:</p> </li> </ol> Directory Manager <code>~/.rbenv/shims</code> rbenv <code>~/.rbenv/bin</code> rbenv <code>~/.rvm/bin</code> rvm <code>~/.asdf/shims</code> asdf <code>/usr/local/bin</code> Homebrew / manual <code>/usr/bin</code> system <code>/opt/homebrew/bin</code> Homebrew (Apple Silicon) <p>Only an executable file satisfies the check \u2014 a non-executable file at the path is ignored.</p> <p>If <code>aia</code> is not found by either method, all prompts fail this check \u2014 no jobs are installed.</p> <p>The check result is cached per <code>Validator</code> instance (shell spawn is expensive).</p>"},{"location":"guides/validation/#4-prompts-directory","title":"4. Prompts Directory","text":"<p><code>$AIA_PROMPTS_DIR</code> must be set, exist, and be readable. This check happens before scanning, not per-prompt. If it fails, <code>aias</code> exits immediately with an error.</p>"},{"location":"guides/validation/#validation-flow","title":"Validation Flow","text":"<pre><code>For each prompt file with schedule: in frontmatter:\n  1. Check schedule syntax        \u2192 error if invalid\n  2. Check parameter defaults     \u2192 error per missing default\n  3. Check aia binary             \u2192 error if not found (cached)\n\n  If all checks pass  \u2192 prompt is included in install\n  If any check fails  \u2192 prompt is skipped, warning to stderr\n</code></pre> <p>The crontab write happens only after all valid prompts have been collected. A mix of valid and invalid prompts is handled gracefully \u2014 valid ones are installed, invalid ones are skipped.</p>"},{"location":"guides/validation/#seeing-validation-results","title":"Seeing Validation Results","text":"<p>Use <code>aias check</code> to see validation results without modifying the crontab:</p> <pre><code>aias check\n</code></pre> <pre><code>INVALID (would be skipped by update):\n  bad_prompt: Schedule 'every banana': undefined method 'banana' for Integer\n\n  bad_params: Parameter 'topic' has no default value (required for unattended cron execution)\n</code></pre> <p>Use <code>aias dry-run</code> to see what the valid prompts would generate:</p> <pre><code>aias dry-run\n</code></pre>"},{"location":"reference/api/","title":"API Reference","text":"<p>All public classes live under the <code>Aias::</code> namespace and are autoloaded by Zeitwerk when <code>require \"aias\"</code> is called.</p>"},{"location":"reference/api/#aiascli","title":"<code>Aias::CLI</code>","text":"<p>Inherits: <code>Thor</code></p> <p>The command-line interface. All eight commands are public instance methods. <code>CLI.start(ARGV)</code> is the gem entry point called by the <code>aias</code> binary.</p>"},{"location":"reference/api/#class-methods","title":"Class Methods","text":""},{"location":"reference/api/#exit_on_failure","title":"<code>.exit_on_failure?</code>","text":"<p>Returns <code>true</code>. Thor uses this to call <code>exit(1)</code> when a command raises an unhandled error.</p>"},{"location":"reference/api/#instance-methods","title":"Instance Methods","text":""},{"location":"reference/api/#update","title":"<code>#update</code>","text":"<p>Scan, validate, and install all scheduled prompts.</p> <pre><code>cli = Aias::CLI.new\ncli.update\n</code></pre>"},{"location":"reference/api/#addpath","title":"<code>#add(path)</code>","text":"<p>Add or replace a single prompt's cron job. <code>path</code> is an absolute or relative path to the prompt file.</p> <pre><code>cli.add(\"/Users/you/.prompts/standup.md\")\ncli.add(\"./example_prompts/morning_standup.md\")\n</code></pre> <p>Raises <code>SystemExit(1)</code> when the file is missing, outside the prompts directory, has no <code>schedule:</code> key, or fails validation.</p> <p>See <code>aias add</code> for full behaviour and upsert semantics.</p>"},{"location":"reference/api/#check","title":"<code>#check</code>","text":"<p>Print a diff between scheduled prompts and installed jobs.</p> <pre><code>cli.check\n</code></pre>"},{"location":"reference/api/#list","title":"<code>#list</code>","text":"<p>Print a table of all installed jobs.</p> <pre><code>cli.list\n</code></pre>"},{"location":"reference/api/#dry_run","title":"<code>#dry_run</code>","text":"<p>Print the cron output that <code>update</code> would write, without touching the crontab.</p> <pre><code>cli.dry_run\n</code></pre>"},{"location":"reference/api/#showprompt_id","title":"<code>#show(prompt_id)</code>","text":"<p>Print details for a single installed job. Exits 1 if not found.</p> <pre><code>cli.show(\"daily_digest\")\ncli.show(\"reports/weekly\")\n</code></pre>"},{"location":"reference/api/#upcomingn-5","title":"<code>#upcoming(n = \"5\")</code>","text":"<p>Print schedule and last-run information for all installed jobs. Aliased as <code>aias next</code> on the command line.</p> <pre><code>cli.upcoming\ncli.upcoming(\"10\")\n</code></pre>"},{"location":"reference/api/#clear","title":"<code>#clear</code>","text":"<p>Remove all aias-managed crontab entries.</p> <pre><code>cli.clear\n</code></pre>"},{"location":"reference/api/#dependency-injection","title":"Dependency Injection","text":"<p>Collaborators are initialized lazily via private accessors. Set instance variables before invoking a command to inject alternatives:</p> <pre><code>cli = Aias::CLI.new\ncli.instance_variable_set(:@validator, Aias::Validator.new(binary_to_check: \"ruby\"))\ncli.instance_variable_set(:@scanner,  Aias::PromptScanner.new(prompts_dir: \"/tmp/test\"))\ncli.update\n</code></pre>"},{"location":"reference/api/#aiaspromptscanner","title":"<code>Aias::PromptScanner</code>","text":"<p>Discovers AIA prompt files that declare a <code>schedule:</code> key and parses their frontmatter.</p>"},{"location":"reference/api/#result","title":"<code>::Result</code>","text":"<pre><code>Aias::PromptScanner::Result = Data.define(:prompt_id, :schedule, :metadata, :file_path)\n</code></pre> <p>A frozen, immutable value object. Fields:</p> Field Type Description <code>prompt_id</code> <code>String</code> Subpath relative to prompts dir, no <code>.md</code> extension <code>schedule</code> <code>String</code> Raw value of the <code>schedule:</code> frontmatter key <code>metadata</code> <code>PM::Metadata</code> Full parsed frontmatter object <code>file_path</code> <code>String</code> Absolute path to the prompt file"},{"location":"reference/api/#new","title":"<code>.new</code>","text":"<pre><code>scanner = Aias::PromptScanner.new\nscanner = Aias::PromptScanner.new(prompts_dir: \"/path/to/prompts\")\n</code></pre> Parameter Default Description <code>prompts_dir:</code> <code>ENV[\"AIA_PROMPTS_DIR\"]</code> Directory to scan"},{"location":"reference/api/#scan","title":"<code>#scan</code>","text":"<pre><code>results = scanner.scan  # =&gt; Array&lt;Aias::PromptScanner::Result&gt;\n</code></pre> <p>Returns an array of <code>Result</code> objects for every prompt that has a non-empty <code>schedule:</code> key.</p> <p>Raises <code>Aias::Error</code> when:</p> <ul> <li><code>prompts_dir</code> is nil or empty (AIA_PROMPTS_DIR not set)</li> <li>The directory does not exist</li> <li>The directory is not readable</li> </ul> <p>Prompts with unparseable frontmatter are warned to stderr and skipped (no exception).</p>"},{"location":"reference/api/#scan_one","title":"<code>#scan_one</code>","text":"<pre><code>result = scanner.scan_one(path)  # =&gt; Aias::PromptScanner::Result\n</code></pre> <p>Parses a single prompt file by path (absolute or relative). Returns a <code>Result</code> on success.</p> <p>Raises <code>Aias::Error</code> when:</p> Condition Message File does not exist <code>\"Prompt file not found: &lt;path&gt;\"</code> File is not readable <code>\"Prompt file not readable: &lt;path&gt;\"</code> File is outside <code>prompts_dir</code> <code>\"'&lt;path&gt;' is not inside the prompts directory '&lt;dir&gt;'\"</code> No <code>schedule:</code> in frontmatter <code>\"'&lt;prompt_id&gt;' has no schedule: in its frontmatter\"</code> Frontmatter is unparseable <code>\"Failed to parse '&lt;prompt_id&gt;': &lt;original error&gt;\"</code> <code>prompts_dir</code> missing or unreadable Same as <code>#scan</code> <p>Unlike <code>#scan</code>, <code>scan_one</code> never silently skips \u2014 every error condition is a raised exception.</p>"},{"location":"reference/api/#aiasvalidator","title":"<code>Aias::Validator</code>","text":"<p>Validates a <code>PromptScanner::Result</code> against three rules: schedule syntax, parameter completeness, and <code>aia</code> binary presence.</p>"},{"location":"reference/api/#validationresult","title":"<code>::ValidationResult</code>","text":"<pre><code>Aias::Validator::ValidationResult = Data.define(:valid?, :errors)\n</code></pre> <p>A frozen, immutable value object. Fields:</p> Field Type Description <code>valid?</code> <code>Boolean</code> <code>true</code> when all checks pass <code>errors</code> <code>Array&lt;String&gt;</code> Human-readable error messages (empty when valid)"},{"location":"reference/api/#new_1","title":"<code>.new</code>","text":"<pre><code>validator = Aias::Validator.new\nvalidator = Aias::Validator.new(\n  shell:         \"/bin/bash\",\n  binary_to_check: \"aia\",\n  fallback_dirs: Aias::Validator::BINARY_FALLBACK_DIRS\n)\n</code></pre> Parameter Default Description <code>shell:</code> <code>ENV[\"SHELL\"]</code> or <code>\"/bin/bash\"</code> Login shell used for binary check <code>binary_to_check:</code> <code>\"aia\"</code> Binary name to locate <code>fallback_dirs:</code> <code>BINARY_FALLBACK_DIRS</code> Directories scanned when the login-shell check fails <p><code>binary_to_check:</code> and <code>fallback_dirs:</code> are primarily useful in tests \u2014 pass <code>\"ruby\"</code> for a check that always succeeds, or an empty/nonexistent directory list for one that always fails.</p>"},{"location":"reference/api/#binary_fallback_dirs","title":"<code>::BINARY_FALLBACK_DIRS</code>","text":"<pre><code>Aias::Validator::BINARY_FALLBACK_DIRS\n# =&gt; [\n#      \"~/.rbenv/shims\",\n#      \"~/.rbenv/bin\",\n#      \"~/.rvm/bin\",\n#      \"~/.asdf/shims\",\n#      \"/usr/local/bin\",\n#      \"/usr/bin\",\n#      \"/opt/homebrew/bin\"\n#    ]\n</code></pre> <p>The default fallback directory list. Covers rbenv, rvm, asdf, Homebrew (Intel + Apple Silicon), and the standard system paths.</p>"},{"location":"reference/api/#validate","title":"<code>#validate</code>","text":"<pre><code>vr = validator.validate(scanner_result)  # =&gt; Aias::Validator::ValidationResult\nvr.valid?   # =&gt; true or false\nvr.errors   # =&gt; [\"Schedule 'x': ...\", \"Parameter 'y' has no default ...\"]\n</code></pre> <p>The binary check result is memoised per <code>Validator</code> instance and only runs once.</p>"},{"location":"reference/api/#binary_in_fallback_location","title":"<code>#binary_in_fallback_location?</code>","text":"<pre><code>validator.binary_in_fallback_location?  # =&gt; true or false\n</code></pre> <p>Returns <code>true</code> when an executable file named <code>binary_to_check</code> exists in any of the <code>fallback_dirs</code>. Called automatically by <code>#validate</code>; exposed publicly for diagnostics.</p>"},{"location":"reference/api/#aiasjobbuilder","title":"<code>Aias::JobBuilder</code>","text":"<p>Converts a <code>PromptScanner::Result</code> into a raw cron line string. Pure function \u2014 no I/O.</p>"},{"location":"reference/api/#new_2","title":"<code>.new</code>","text":"<pre><code>builder = Aias::JobBuilder.new\nbuilder = Aias::JobBuilder.new(shell: \"/bin/zsh\")\nbuilder = Aias::JobBuilder.new(shell: \"/bin/zsh\", prompts_dir: \"/path/to/prompts\")\n</code></pre> Parameter Default Description <code>shell:</code> <code>ENV[\"SHELL\"]</code> or <code>\"/bin/bash\"</code> Login shell binary used to wrap the <code>aia</code> invocation <code>prompts_dir:</code> <code>nil</code> When set, appends <code>--prompts-dir DIR</code> to every generated <code>aia</code> command"},{"location":"reference/api/#build","title":"<code>#build</code>","text":"<pre><code>cron_line = builder.build(scanner_result)  # =&gt; String\n</code></pre> <p>Returns a fully-formed cron line string suitable for passing to <code>CrontabManager#install</code> or <code>#add_job</code>. Uses <code>fugit</code> to resolve the <code>schedule:</code> value to a canonical 5-field cron expression.</p> <p>Without <code>prompts_dir</code>:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia daily_digest &gt;&gt; /Users/you/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n</code></pre> <p>With <code>prompts_dir: \"/data/prompts\"</code>:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia --prompts-dir /data/prompts daily_digest &gt;&gt; /Users/you/.aia/schedule/logs/daily_digest.log 2&gt;&amp;1'\n</code></pre>"},{"location":"reference/api/#log_path_for","title":"<code>#log_path_for</code>","text":"<pre><code>builder.log_path_for(\"daily_digest\")     # =&gt; \"/Users/you/.aia/schedule/logs/daily_digest.log\"\nbuilder.log_path_for(\"reports/weekly\")   # =&gt; \"/Users/you/.aia/schedule/logs/reports/weekly.log\"\n</code></pre> <p>Returns the absolute log path for a given prompt ID.</p>"},{"location":"reference/api/#aiascrontabmanager","title":"<code>Aias::CrontabManager</code>","text":"<p>Manages the aias-owned block in the user's crontab by directly invoking the system <code>crontab(1)</code> command via <code>Open3</code>.</p>"},{"location":"reference/api/#constants","title":"Constants","text":"Constant Value Description <code>IDENTIFIER</code> <code>\"aias\"</code> Whenever identifier that marks the managed block <code>LOG_BASE</code> <code>~/.aia/schedule/logs</code> Default base directory for log files <code>ENTRY_RE</code> Regexp Parses installed cron lines to extract <code>prompt_id</code>, <code>cron_expr</code>, <code>log_path</code>"},{"location":"reference/api/#new_3","title":"<code>.new</code>","text":"<pre><code>manager = Aias::CrontabManager.new\nmanager = Aias::CrontabManager.new(crontab_command: \"crontab\", log_base: \"/custom/log/base\")\n</code></pre> Parameter Default Description <code>crontab_command:</code> <code>\"crontab\"</code> Path to the <code>crontab(1)</code> binary <code>log_base:</code> <code>LOG_BASE</code> Base directory for log files <p>The <code>crontab_command:</code> parameter makes the manager testable with a fake crontab script backed by a tmpfile, with no system crontab involvement.</p>"},{"location":"reference/api/#install","title":"<code>#install</code>","text":"<pre><code>manager.install(cron_lines)\n</code></pre> <p>Accepts a single cron line string or an array of cron line strings. Creates <code>@log_base</code> if absent, then replaces the entire <code>aias</code>-managed crontab block by piping the new content to <code>crontab -</code> via <code>Open3</code>.</p> <p>Raises <code>Aias::Error</code> if the <code>crontab</code> command exits non-zero.</p>"},{"location":"reference/api/#add_job","title":"<code>#add_job</code>","text":"<pre><code>manager.add_job(cron_line, prompt_id)\n</code></pre> <p>Upserts a single cron entry into the aias-managed block. Any existing entry whose <code>prompt_id</code> matches is removed first; the new <code>cron_line</code> is appended. All other managed entries are left unchanged. Non-aias crontab entries are never touched.</p> <p><code>cron_line</code> is a fully-formed cron line as produced by <code>JobBuilder#build</code>:</p> <pre><code>0 9 * * 1-5 /bin/zsh -l -c 'aia standup &gt;&gt; ~/.aia/schedule/logs/standup.log 2&gt;&amp;1'\n</code></pre> <p>Creates <code>@log_base</code> if absent. Raises <code>Aias::Error</code> if the crontab write fails.</p>"},{"location":"reference/api/#clear_1","title":"<code>#clear</code>","text":"<pre><code>manager.clear\n</code></pre> <p>Removes the <code># BEGIN aias</code> \u2026 <code># END aias</code> block from the crontab and rewrites it via <code>crontab -</code>. Non-aias entries are untouched.</p>"},{"location":"reference/api/#dry_run_1","title":"<code>#dry_run</code>","text":"<pre><code>output = manager.dry_run(cron_lines)  # =&gt; String\n</code></pre> <p>Returns the cron lines that would be installed joined by newlines, without making any system calls or writing to the crontab.</p>"},{"location":"reference/api/#installed_jobs","title":"<code>#installed_jobs</code>","text":"<pre><code>jobs = manager.installed_jobs\n# =&gt; [\n#      { prompt_id: \"daily_digest\", cron_expr: \"0 8 * * *\", log_path: \"/...\" },\n#      { prompt_id: \"reports/weekly\", cron_expr: \"0 9 * * 1\", log_path: \"/...\" }\n#    ]\n</code></pre> <p>Reads the current crontab and parses the <code>aias</code>-managed block. Returns an empty array when no block is installed.</p>"},{"location":"reference/api/#current_block","title":"<code>#current_block</code>","text":"<pre><code>block = manager.current_block  # =&gt; String\n</code></pre> <p>Returns the raw text between the <code># BEGIN aias</code> and <code># END aias</code> marker lines (markers excluded). Returns <code>\"\"</code> when no block exists.</p>"},{"location":"reference/api/#ensure_log_directories","title":"<code>#ensure_log_directories</code>","text":"<pre><code>manager.ensure_log_directories([\"daily_digest\", \"reports/weekly\"])\n</code></pre> <p>Creates the log subdirectory structure under <code>@log_base</code> for each prompt ID. Called by <code>CLI#update</code> before installing.</p>"},{"location":"reference/api/#aiaserror","title":"<code>Aias::Error</code>","text":"<pre><code>class Aias::Error &lt; StandardError; end\n</code></pre> <p>Raised for unrecoverable errors: missing prompts directory, crontab write failure. Caught by <code>CLI</code> which prints the message and exits 1.</p>"},{"location":"reference/architecture/","title":"Architecture","text":""},{"location":"reference/architecture/#overview","title":"Overview","text":"<p><code>aias</code> is a thin orchestration layer over three well-established tools: <code>grep</code> (for discovery), <code>PM::Metadata</code> from the <code>prompt_manager</code> gem (for frontmatter parsing), and <code>fugit</code> (for cron expression parsing and natural-language schedule resolution). Its five classes have narrow, well-defined responsibilities.</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                         aias gem                                \u2502\n\u2502                                                                 \u2502\n\u2502   Aias::CLI                                                     \u2502\n\u2502   \u251c\u2500\u2500 Aias::PromptScanner   grep -rl + PM::Metadata             \u2502\n\u2502   \u251c\u2500\u2500 Aias::Validator       schedule syntax + params + binary   \u2502\n\u2502   \u251c\u2500\u2500 Aias::JobBuilder      prompt ID + schedule \u2192 cron line    \u2502\n\u2502   \u2514\u2500\u2500 Aias::CrontabManager  crontab(1) read/write               \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"reference/architecture/#data-flow","title":"Data Flow","text":"<pre><code>$AIA_PROMPTS_DIR\n      \u2502\n      \u25bc\nPromptScanner#scan\n      \u2502 returns Array&lt;PromptScanner::Result&gt;\n      \u2502   Result = Data.define(:prompt_id, :schedule, :metadata, :file_path)\n      \u25bc\nValidator#validate(result)\n      \u2502 returns ValidationResult = Data.define(:valid?, :errors)\n      \u25bc\n[partition into valid / invalid]\n      \u2502\n      \u251c\u2500\u2500 invalid \u2192 warn to stderr, skip\n      \u2502\n      \u2514\u2500\u2500 valid \u2500\u2500\u25ba  JobBuilder#build(result)\n                         \u2502 returns String (raw cron line)\n                         \u25bc\n                    CrontabManager#install(cron_lines)\n                         \u2502 writes via crontab(1) command\n                         \u25bc\n                    crontab (OS cron daemon manages execution)\n</code></pre>"},{"location":"reference/architecture/#class-responsibilities","title":"Class Responsibilities","text":""},{"location":"reference/architecture/#aiascli","title":"<code>Aias::CLI</code>","text":"<p>The entry point. A <code>Thor</code> subclass that exposes eight commands. CLI delegates all domain logic to its four collaborators; it only formats output and handles errors.</p> <p>Collaborators are created lazily and accessible via private accessors (<code>scanner</code>, <code>validator</code>, <code>builder</code>, <code>manager</code>). Tests inject replacements by setting instance variables before calling a command.</p>"},{"location":"reference/architecture/#aiaspromptscanner","title":"<code>Aias::PromptScanner</code>","text":"<p>Discovers scheduled prompts in two steps:</p> <ol> <li>grep phase: runs <code>grep -rl \"schedule:\" $AIA_PROMPTS_DIR</code> via <code>Open3.capture3</code> (no shell injection)</li> <li>parse phase: for each candidate file, calls <code>PM.parse(absolute_path)</code> and reads <code>metadata.schedule</code></li> </ol> <p>Returns an array of frozen <code>Result</code> value objects. Raises <code>Aias::Error</code> if the prompts directory is missing or unreadable.</p>"},{"location":"reference/architecture/#aiasvalidator","title":"<code>Aias::Validator</code>","text":"<p>Validates a single <code>PromptScanner::Result</code> against three rules (see Validation). Returns a frozen <code>ValidationResult</code> value object.</p> <p>The <code>aia</code> binary check is memoised \u2014 it runs at most once per <code>Validator</code> instance.</p> <p>The <code>binary_to_check:</code> constructor parameter makes the binary check testable without stubs.</p>"},{"location":"reference/architecture/#aiasjobbuilder","title":"<code>Aias::JobBuilder</code>","text":"<p>Pure function: converts a <code>PromptScanner::Result</code> into a raw cron line string. No I/O, no side effects.</p> <p>Uses <code>fugit</code> to resolve the <code>schedule:</code> value to a canonical 5-field cron expression, then assembles:</p> <pre><code>&lt;cron_expr&gt; &lt;shell&gt; -l -c 'aia [--prompts-dir DIR] &lt;prompt_id&gt; &gt;&gt; &lt;log&gt; 2&gt;&amp;1'\n</code></pre> <p>Uses <code>ENV[\"SHELL\"]</code> (defaulting to <code>/bin/bash</code>) as the login shell. Log paths follow the pattern <code>~/.aia/schedule/logs/&lt;prompt_id&gt;.log</code>.</p>"},{"location":"reference/architecture/#aiascrontabmanager","title":"<code>Aias::CrontabManager</code>","text":"<p>Manages the aias-owned block in the user's crontab by directly invoking the system <code>crontab(1)</code> command via <code>Open3</code>. Three categories of operations:</p> Category Methods How Write <code>install(cron_lines)</code>, <code>add_job(cron_line, prompt_id)</code>, <code>clear</code> <code>Open3.popen2(crontab_command, \"-\")</code> \u2014 pipes new content to stdin Read <code>installed_jobs</code>, <code>current_block</code> <code>Open3.capture3(crontab_command, \"-l\")</code> Preview <code>dry_run(cron_lines)</code> <code>Array(cron_lines).join(\"\\n\")</code> \u2014 no system calls <p>The managed block is delimited by <code># BEGIN aias</code> and <code># END aias</code> marker comments. All other crontab entries are preserved verbatim.</p> <p>The <code>crontab_command:</code> and <code>log_base:</code> constructor parameters make the manager fully testable with a fake crontab script backed by a tmpfile \u2014 no system crontab is ever touched during tests.</p>"},{"location":"reference/architecture/#key-value-objects","title":"Key Value Objects","text":"<p>Both value objects are defined with <code>Data.define</code> (Ruby 3.2+) and are automatically frozen and immutable.</p> <pre><code># Returned by PromptScanner#scan\nAias::PromptScanner::Result = Data.define(:prompt_id, :schedule, :metadata, :file_path)\n\n# Returned by Validator#validate\nAias::Validator::ValidationResult = Data.define(:valid?, :errors)\n</code></pre>"},{"location":"reference/architecture/#autoloading","title":"Autoloading","text":"<p>Zeitwerk handles autoloading. The <code>cli</code> filename is mapped to <code>Aias::CLI</code> (not <code>Aias::Cli</code>) via a custom inflection:</p> <pre><code>loader = Zeitwerk::Loader.for_gem\nloader.inflector.inflect(\"cli\" =&gt; \"CLI\")\nloader.setup\n</code></pre>"},{"location":"reference/architecture/#error-handling","title":"Error Handling","text":"<p>A single <code>Aias::Error &lt; StandardError</code> is raised for unrecoverable conditions (missing prompts dir, crontab write failure). The CLI rescues it, prints the message, and exits 1.</p> <p>Recoverable per-prompt issues (invalid schedule, missing parameter defaults) are collected as error strings in <code>ValidationResult#errors</code> and never raised as exceptions.</p>"},{"location":"reference/environment/","title":"Environment Variables","text":""},{"location":"reference/environment/#prompts-directory-resolution","title":"Prompts Directory Resolution","text":"<p>The prompts directory is resolved in this order:</p> Priority Source 1 (highest) <code>--prompts-dir PATH</code> CLI option 2 <code>AIA_PROMPTS__DIR</code> environment variable (AIA &gt;= 0.8.0) 3 <code>AIA_PROMPTS_DIR</code> environment variable (AIA &lt; 0.8.0) \u2014 Error if none of the above is set"},{"location":"reference/environment/#aia_prompts__dir","title":"<code>AIA_PROMPTS__DIR</code>","text":"<p>Preferred. Set by AIA &gt;= 0.8.0. Takes precedence over <code>AIA_PROMPTS_DIR</code>.</p> <pre><code>export AIA_PROMPTS__DIR=\"$HOME/.prompts\"\n</code></pre>"},{"location":"reference/environment/#aia_prompts_dir","title":"<code>AIA_PROMPTS_DIR</code>","text":"<p>Legacy (AIA &lt; 0.8.0). The absolute path to the directory containing your AIA prompt files. Used as a fallback when <code>AIA_PROMPTS__DIR</code> is not set.</p> <pre><code>export AIA_PROMPTS_DIR=\"$HOME/.prompts\"\n</code></pre> <p>Set this in your login profile (<code>.zprofile</code>, <code>.bash_profile</code>) so it is available in cron jobs.</p>"},{"location":"reference/environment/#shell","title":"<code>SHELL</code>","text":"<p>Optional. The shell binary used to wrap each cron entry with a login invocation.</p> <p><code>aias</code> reads <code>ENV[\"SHELL\"]</code> when building job entries. If not set, falls back to <code>/bin/bash</code>.</p> <pre><code>echo $SHELL\n# /bin/zsh\n</code></pre> <p>This produces cron entries of the form:</p> <pre><code>0 8 * * * /bin/zsh -l -c 'aia daily_digest &gt;&gt; ... 2&gt;&amp;1'\n</code></pre> <p>To override the shell for a specific <code>update</code> run:</p> <pre><code>SHELL=/bin/bash aias update\n</code></pre>"},{"location":"reference/environment/#variables-needed-by-aia-at-runtime","title":"Variables Needed by AIA at Runtime","text":"<p>These are not used by <code>aias</code> directly, but must be available in your login profile for cron jobs to succeed.</p> Variable Description <code>ANTHROPIC_API_KEY</code> Anthropic API key (when using Claude) <code>OPENAI_API_KEY</code> OpenAI API key (when using GPT models) <code>AIA_PROMPTS_DIR</code> Same as above \u2014 must be in login profile <p>Set these in your login profile, not only in your interactive shell's rc file (<code>.bashrc</code>, <code>.zshrc</code>). See Cron Environment for details.</p>"},{"location":"reference/logging/","title":"Logging","text":""},{"location":"reference/logging/#log-file-location","title":"Log File Location","text":"<p>Each scheduled prompt writes to its own log file under <code>~/.aia/schedule/logs/</code>:</p> <pre><code>~/.aia/schedule/logs/\n  daily_digest.log\n  reports/\n    weekly.log\n    monthly_summary.log\n</code></pre> <p>The subdirectory structure mirrors the prompt's subpath relative to <code>$AIA_PROMPTS_DIR</code>.</p> Prompt ID Log File <code>daily_digest</code> <code>~/.aia/schedule/logs/daily_digest.log</code> <code>reports/weekly</code> <code>~/.aia/schedule/logs/reports/weekly.log</code> <code>a/b/deep</code> <code>~/.aia/schedule/logs/a/b/deep.log</code>"},{"location":"reference/logging/#log-behaviour","title":"Log Behaviour","text":"Behaviour Details Always append <code>&gt;&gt;</code> is used \u2014 log entries accumulate across runs Combined stdout + stderr <code>2&gt;&amp;1</code> \u2014 all output goes to the same file Created by cron The log file is created on the first run; <code>aias</code> only creates the parent directory"},{"location":"reference/logging/#log-directory-creation","title":"Log Directory Creation","text":"<p><code>aias update</code> calls <code>ensure_log_directories</code> before installing, which creates the necessary subdirectory structure under <code>~/.aia/schedule/logs/</code>. The directories are created even if no jobs have run yet.</p>"},{"location":"reference/logging/#viewing-logs","title":"Viewing Logs","text":"<pre><code># Tail the log for a specific prompt\ntail -f ~/.aia/schedule/logs/daily_digest.log\n\n# Check the last run time\nls -la ~/.aia/schedule/logs/daily_digest.log\n\n# View recent entries\ntail -n 50 ~/.aia/schedule/logs/reports/weekly.log\n</code></pre>"},{"location":"reference/logging/#using-aias-next","title":"Using aias next","text":"<p>The <code>aias next</code> command shows the log file's modification timestamp as a proxy for the last run time:</p> <pre><code>aias next\n</code></pre> <pre><code>daily_digest\n  schedule : 0 8 * * *\n  last run : 2025-03-20 08:00:01 +0000\n  log      : /Users/you/.aia/schedule/logs/daily_digest.log\n</code></pre> <p>\"never run\" is shown when the log file does not exist.</p>"},{"location":"reference/logging/#customising-the-log-base","title":"Customising the Log Base","text":"<p>The log base directory defaults to <code>~/.aia/schedule/logs</code>. It can be overridden when constructing <code>CrontabManager</code> directly:</p> <pre><code>manager = Aias::CrontabManager.new(log_base: \"/custom/log/dir\")\n</code></pre> <p>This is intended for testing and programmatic use. The CLI always uses the default.</p>"}]}